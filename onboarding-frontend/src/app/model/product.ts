export interface Product {

    idproduct: number;
    name: string;
	code: string;
	description: string;
	price: number;
	stock: number;
	image: string;

	/*
	public constructor(idproduct : number, name: string, code: string, description: string, price: number, stock:number, image: string){

	}
	*/
}